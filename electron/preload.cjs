// Intentionally minimal: JARVIS's renderer only talks to its own local
// Express backend over HTTP, so no privileged Node/Electron APIs need to be
// exposed to the page. Kept as a placeholder for future desktop-only
// integrations (e.g. native notifications, tray icon controls).
